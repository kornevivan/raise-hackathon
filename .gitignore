# python
backend/.venv/
__pycache__/
*.pyc
# NOTE: backend/.llm_cache is intentionally COMMITTED — pre-warmed Vultr responses
# so the demo replays instantly and live (see README "Offline vs live Vultr").

# node
frontend/node_modules/
frontend/dist/

# env / secrets
.env
backend/.env

# os
.DS_Store

# keep the deterministic corpus + sqlite committed (they ARE the demo data):
# backend/data/corpus/**, backend/data/*.json, backend/data/*.csv, backend/data/*.sqlite

# hackathon source briefs (kept locally, not part of the shipped repo)
Details _ RAISE Summit Hackathon.html
RAISE HACKATHON TRACK_ Agentic Intelligence with the Vultron Retriever.pdf


# runtime uploads (regenerated)
backend/data/uploads/

# rework intake (large source zips kept local)
_incoming/
testing-data.zip
rework-instructions-v2.md
backend/data/uploads/

backend/data/runs.sqlite

# real SEC filings are committed (public; A0). HTML intermediates ignored:
backend/data/real/*.html

# local-only dev intake / briefs / scratch (never shipped)
*.png
_devreq/
descripting-instructions*.md
dev-requirements.zip
covenant-sentinel-spec*.md
Hackathon.html
Retriever.pdf
